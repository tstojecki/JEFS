jefs.js is built using the msbuilt task. 

You should work on the script files in the ($ProjectDir)Source folder. 
The files are built and minified (Release mode only) automatically when the project is built.